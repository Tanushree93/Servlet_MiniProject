# Servlet_MiniProject
“User Management System is a Java Servlet–based web application that performs CRUD operations with pagination using JDBC and MySQL. It allows users to register, log in, view records page-wise, update and delete data. HTML pages are secured inside WEB-INF and accessed via servlets.”
